# 📜 SCRIPT-GUIDED SAMANTHA EXTRACTION

This is the **Zero-Compromise Method**. It uses the movie screenplay as Ground Truth to identify Samantha, and Demucs AI to strip the background music.

## 🛠️ SETUP

1. **Install Dependencies:**
   ```bash
   pip install -r requirements_script_guided.txt
   # Also ensure you have FFmpeg installed (sudo apt install ffmpeg)